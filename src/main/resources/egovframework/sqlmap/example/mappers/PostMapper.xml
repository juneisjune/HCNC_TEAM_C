<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="hcnc.cteam.post.PostMapper">
    <!-- 게시글 목록 조회 -->
    <select id="selectPost" parameterType="map" resultType="map">
	    SELECT a.post_code, a.title, a.content, a.reg_date, b.name AS emp_name
	    	FROM post a
	    INNER JOIN employee b 
	      ON a.emp_code = b.emp_code
	</select>
	<!-- 게시글 상세 조회 관련 쿼리 -->	
	<select id="selectPostDetail" parameterType="int" resultType="map">
	    SELECT a.post_code, a.title, content, reg_date, emp_code
	    FROM post a
	    WHERE post_code = #{postCode}
	</select>	
	
</mapper>

